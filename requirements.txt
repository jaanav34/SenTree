# Dashboard-only dependencies (Streamlit Cloud reads this file)
# For the full pipeline (torch, torch-geometric, xarray, etc.) use:
#   pip install -r requirements-pipeline.txt
numpy>=1.24
pandas>=2.0
scipy>=1.11
matplotlib>=3.7
Pillow>=10.0
streamlit>=1.30
altair>=5.0
pydeck>=0.8
chromadb>=0.4
google-genai>=0.4
scikit-learn>=1.3
